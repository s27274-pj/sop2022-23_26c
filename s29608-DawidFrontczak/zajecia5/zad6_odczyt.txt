#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
 
int main() {
	FILE *plik;
	char wiadomosc[128];
 
	while (1) {
		plik = fopen("/tmp/chat", "r");
		while (fgets(wiadomosc, sizeof(wiadomosc), plik)) {
			printf("%s", wiadomosc);
		}
 
		fclose(plik);
		sleep(1);
	}
 
	return 0;
}
